# Math Games Demo

The demo gh-pages site is built from `demo` branch, `docs` folder.

## to test locally: 

set up ruby and github-pages gem

```
gem install github-pages
```

Then construct and test the site locally

```
cd docs
jekyll build
jekyll serve
```
